#include<stdio.h>
#include"mymath.h"
int main()
{
	int(no1=32,no2=8,result;
	result=sum(no1,no2);
	printf("sum of two numbers is %d\n",result);
	result=diff(no1,no2);
	printf("diff of two numbers is %d\n",result);
	return 0;
}

